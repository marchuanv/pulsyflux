{
  "name": "pulsyflux-broker",
  "version": "2.0.0",
  "description": "Node.js bindings for PulsyFlux Broker - high-performance pub/sub messaging",
  "main": "index.mjs",
  "type": "module",
  "scripts": {
    "preinstall": "clean.bat",
    "postinstall": "npm run build",
    "build": "npm run build:go && npx tsx build.mjs",
    "build:go": "build.bat",
    "test": "jasmine --config=spec/support/jasmine.json"
  },
  "dependencies": {
    "node-addon-api": "^8.5.0",
    "zig-build": "github:solarwinds/zig-build"
  },
  "devDependencies": {
    "jasmine": "^5.13.0",
    "prebuildify": "^6.0.1",
    "tsx": "^4.21.0"
  },
  "trustedDependencies": [
    "zig-build"
  ],
  "keywords": [
    "broker",
    "pubsub",
    "messaging",
    "events",
    "tcp",
    "multiplexing",
    "high-performance"
  ],
  "author": "",
  "license": "MIT"
}
